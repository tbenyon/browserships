# Browserships

Sample app for my first experience of both front end and back end development.

The front end uses HTML and Javascript with Angular.

The back server is written in Node with Express.


Getting started:
In the directory the app is stored, execute "npm install" from the terminal.
